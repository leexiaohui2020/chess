{
  "name": "chess",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build -w"
  },
  "devDependencies": {
    "@types/events": "^3.0.0",
    "typescript": "^4.2.3"
  }
}
